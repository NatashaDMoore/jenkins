{

    "plus-minus": {
        "regex": "^[+-]",
        "should-match": "should-match.txt",
        "should-not-match": ".txt",
        "test-include": null,
        "test-exclude": null,
        "test-combined": "test.sh"
    },    
    "git": {
        "regex": "^\\S+\\s+\\(.*\\)\\s+\\S+\\s+\\S+\\s+\\[.*\\].*$",
        "should-match": "should-match.txt",
        "should-not-match": "should-not-match.txt",
        "test-include": null,
        "test-exclude": null,
        "test-combined": "test.sh"
    },
    "auth": {
        "regex": ".*sshd.*session.*",
        "should-match": "should-match.txt",
        "should-not-match": "should-not-match.txt",
        "test-include": null,
        "test-exclude": null,
        "test-combined": "test.sh"
    }
}